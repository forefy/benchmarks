# Case 012 - Profile PDA Seeds

This case models user profile creation under a username and domain namespace.

Audit focus:
- Whether PDA seed material is unambiguous.
- Whether variable-length user input is length-bounded.
- Whether distinct logical namespaces can map to the same derived address.
