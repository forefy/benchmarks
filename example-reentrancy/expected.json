[
  {
    "id": "case-001",
    "input": {
      "contract": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.0;\n\ncontract Vulnerable {\n    mapping(address => uint256) public balances;\n\n    function deposit() public payable {\n        balances[msg.sender] += msg.value;\n    }\n\n    function withdraw() public {\n        uint256 amount = balances[msg.sender];\n        require(amount > 0);\n        (bool success, ) = msg.sender.call{value: amount}(\"\");\n        require(success);\n        balances[msg.sender] = 0;\n    }\n}"
    },
    "expected_output": {
      "findings": ["reentrancy at line 14 - external call before state update in withdraw()"],
      "severity": "critical"
    }
  },
  {
    "id": "case-002",
    "input": {
      "contract": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.0;\n\ncontract Safe {\n    mapping(address => uint256) public balances;\n\n    function deposit() public payable {\n        balances[msg.sender] += msg.value;\n    }\n\n    function withdraw() public {\n        uint256 amount = balances[msg.sender];\n        require(amount > 0);\n        balances[msg.sender] = 0;\n        (bool success, ) = msg.sender.call{value: amount}(\"\");\n        require(success);\n    }\n}"
    },
    "expected_output": {
      "findings": [],
      "severity": "none"
    }
  },
  {
    "id": "case-003",
    "input": {
      "contract": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.0;\n\ninterface IPool {\n    function getBalance(address) external view returns (uint256);\n}\n\ncontract CrossFunction {\n    mapping(address => uint256) private _balances;\n    IPool public pool;\n\n    function claimRewards() external {\n        uint256 reward = pool.getBalance(msg.sender);\n        (bool ok, ) = msg.sender.call{value: reward}(\"\");\n        require(ok);\n        _balances[msg.sender] = 0;\n    }\n\n    function emergencyWithdraw() external {\n        uint256 bal = _balances[msg.sender];\n        claimRewards();\n        _balances[msg.sender] = bal;\n    }\n}"
    },
    "expected_output": {
      "findings": [
        "reentrancy at line 13 - external call before state update in claimRewards()",
        "cross-function reentrancy in emergencyWithdraw() via claimRewards()"
      ],
      "severity": "critical"
    }
  },
  {
    "id": "case-004",
    "input": {
      "contract": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.0;\n\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract Protected is ReentrancyGuard {\n    mapping(address => uint256) public balances;\n\n    function deposit() public payable {\n        balances[msg.sender] += msg.value;\n    }\n\n    function withdraw() public nonReentrant {\n        uint256 amount = balances[msg.sender];\n        require(amount > 0);\n        (bool success, ) = msg.sender.call{value: amount}(\"\");\n        require(success);\n        balances[msg.sender] = 0;\n    }\n}"
    },
    "expected_output": {
      "findings": [],
      "severity": "none"
    }
  },
  {
    "id": "case-005",
    "input": {
      "contract": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.0;\n\ncontract ReadOnly {\n    uint256 public totalSupply;\n    mapping(address => uint256) public shares;\n\n    function getPrice() public view returns (uint256) {\n        return address(this).balance / totalSupply;\n    }\n\n    function flashLoan(uint256 amount) external {\n        uint256 priceBefore = getPrice();\n        (bool ok, ) = msg.sender.call{value: amount}(\"\");\n        require(ok);\n        require(getPrice() >= priceBefore, \"price manipulated\");\n    }\n\n    function liquidate(address user) external {\n        uint256 price = getPrice();\n        require(price < shares[user], \"not undercollateralized\");\n        shares[user] = 0;\n    }\n}"
    },
    "expected_output": {
      "findings": [
        "read-only reentrancy in flashLoan() - getPrice() can be manipulated during external call at line 14"
      ],
      "severity": "high"
    }
  }
]
